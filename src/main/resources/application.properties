# =========================
# DATABASE (MySQL)
# =========================
# Local: localhost:3306, travelu_db, root / 12345
# Cloud (Render, Docker, Kubernetes): override with DB_* env vars

spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:travelu_db}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:12345}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# =========================
# JWT
# =========================
app.jwt.secret=${JWT_SECRET:ThisIsTravelUBackendSecretKeyLongEnough123}
app.jwt.expiration-ms=${JWT_EXPIRATION:86400000}

# =========================
# SERVER PORT
# =========================
# Local: 8081
# Render / Docker: if PORT env var is set, adhi use avutundi

server.port=${PORT:8081}

# =========================
# GMAIL SMTP (FOR PASSWORD RESET)
# =========================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:ushakirankoppada8883@gmail.com}
spring.mail.password=${MAIL_PASSWORD:osxghkgaseqlxyus}   # <-- ikkada app password later pettaali
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.from=${MAIL_FROM:TravelEase <ushakirankoppada8883@gmail.com>}
